version: '3.8'
services:
  app:
    image: servicerouter
    ports:
      - "3090:3090"
    environment:
      - DB_HOST=103.151.33.111
      - DB_PORT=3306
      - DB_USER=root
      - DB_PASSWORD=hesoyam404@
    networks:
      - servicerouter
    command: [ "node", "index.js" ]

networks:
  servicerouter:
    external: true
    driver: bridge
